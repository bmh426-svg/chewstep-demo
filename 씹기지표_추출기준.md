# Chew_Step 데모버전 — 얼굴 랜드마크 씹기 지표 추출 기준

> **문서 버전 v1.0** · 2026-07-14 작성
> 대상 코드: `데모버전/index.html` (demo v17) 내 `<script type="module">` 씹기 지표 추출기
> 엔진: MediaPipe **FaceLandmarker** (CDN `@mediapipe/tasks-vision@0.10.14`, float16 모델)

---

## 0. 한눈에 보기

- 영상은 **서버로 전송하지 않음.** 브라우저 안에서 얼굴 blendshape 점수(0~1)만 추출 → 숫자(`video_metrics`) + 시계열(`video_series`)만 저장. 영상 픽셀은 기기를 떠나지 않는다.
- **핵심 기준**: `jawOpen` 값에 **히스테리시스(열림 0.15 / 닫힘 0.06)** 를 걸어 씹기 1회를 카운트.
- 저장 시 `algo` 버전과 `thresholds`를 함께 남겨, 나중에 임계값을 바꿔도 저장된 시계열로 **재계산 가능**.

---

## 1. 파이프라인 설정

| 항목 | 값 | 설명 |
|---|---|---|
| 엔진 | FaceLandmarker | `numFaces: 1`, `outputFaceBlendshapes: true` |
| 모델 | face_landmarker float16 v1 | Google 호스팅 `.task` 모델 |
| 실행 모드 | VIDEO (`detectForVideo`) | media-time(ms) 기준 프레임 판정 |
| 샘플링 주기 | `SAMPLE_DT = 1/12` | media-time 기준 **초당 약 12샘플** |
| 재생 속도 | `playbackRate = 2` | 2배속 (인앱 WebView는 프레임 유실 커서 낮춤) |
| 프레임 소스 | `requestVideoFrameCallback` 우선 | 미지원/정체 시 `setInterval(60ms)` 폴백 |
| 캔버스 축소 | 긴 변 최대 480px | 처리 속도 확보 |
| 워치독 | `max(25s, (duration+10)s)` | 무한 대기 방지 |

> **v2 완화(demo v12~)**: "정면 아니면 추출 안 됨" 문제 해결을 위해 감지 신뢰도·품질게이트·샘플링을 완화. 단, **씹기 카운트 임계값(0.15/0.06)은 그대로 유지**해 기존 수집 데이터와 호환. 알고리즘 태그만 `hysteresis-open0.15-close0.06-v2-relaxed`로 구분.

---

## 2. 사용하는 blendshape 신호 (프레임당 5개)

프레임마다 `faceBlendshapes[0].categories`에서 점수(0~1)를 읽는다.

| 신호(categoryName) | 저장 배열 | 용도 |
|---|---|---|
| `jawOpen` | `JO` (jaw_open) | **씹기 카운트**의 핵심 (입 벌림) |
| `jawLeft` | `JL` (jaw_left) | 좌우 저작 균형 |
| `jawRight` | `JR` (jaw_right) | 좌우 저작 균형 |
| `tongueOut` | `TO` (tongue_out) | 혀 내밀기(설출) — **실험 지표** |
| `mouthClose` | `LC` | 입술 다물기 (참고) |

---

## 3. 씹기 카운트 — 히스테리시스 상태 머신 (핵심 기준)

임계값: `thresholds = { open: 0.15, close: 0.06, tongue_out: 0.3 }`

```
상태(state) 초기값: 'closed'

if (state === 'closed' && jawOpen >= 0.15)  →  state = 'open'
if (state === 'open'   && jawOpen <= 0.06)  →  chew++  (씹기 1회), state = 'closed'
```

- 열림 임계값(0.15)과 닫힘 임계값(0.06)을 **다르게** 둬서, 값이 문턱 근처에서 떨릴 때 카운트가 튀는 것을 방지 = **히스테리시스**.
- 입을 크게 벌렸다(≥0.15) 확실히 다물어야(≤0.06) 1회로 인정 → 한 번의 "벌림→닫힘" = 씹기 1회.
- 이 임계값은 로컬 추출기 `chew_extract_local.py`와 **동일**. 데이터 호환성 위해 고정.

**혀 내밀기(설출)**: `tongueOut >= 0.3`인 프레임을 1회로 집계 (실험 지표).

---

## 4. 산출 지표 (metrics)

| 필드 | 계산 방식 | 의미 |
|---|---|---|
| `chew_count` | 히스테리시스 카운트 누적 | 관찰 구간 총 씹기 횟수 |
| `chews_per_min` | `chew / observed_sec * 60` | **분당 씹기 횟수** (관찰구간 기준) |
| `avg_jaw_open` | `JO` 평균 | 평균 입 벌림 |
| `max_jaw_open` | `JO` 최댓값 | 최대 입 벌림 |
| `mouth_open_ratio` | `jawOpen≥0.15` 프레임 비율 % | 입 움직임(활동) 비율 |
| `left_pct` / `right_pct` | `avg(JL) / (avg(JL)+avg(JR))` | **좌우 저작 균형** (합 100%) |
| `tongue_thrust_ratio` | `tongueOut≥0.3` 비율 % | 설출 비율 (실험 지표) |
| `avg_lip_close` | `LC` 평균 | 입술 다물기 평균 |
| `detect_rate` | `faces / samples * 100` | **얼굴 검출률 %** |
| `duration_sec` | 영상 길이 | — |
| `observed_sec` | 첫~끝 샘플 시간차 | 실제 관찰 구간(초) |
| `samples` | 총 샘플 수 | — |
| `quality` | 아래 §5 규칙 | 영상 적합도 등급 |
| `algo` | `ALGO_VERSION` 문자열 | 재계산용 알고리즘 태그 |
| `thresholds` | `{open, close, tongue_out}` | 재계산용 임계값 스냅샷 |

시계열(`series`)은 `t / jaw_open / jaw_left / jaw_right / tongue_out`을 **최대 300점으로 다운샘플**해 저장.

---

## 5. 품질 게이트 (영상이 씹기 분석에 쓸 만한지)

우선순위 순으로 판정 (v2에서 문턱 완화):

| 등급 | 조건 | 의미 |
|---|---|---|
| `low_face` | `faces === 0` 또는 `detect_rate < 35` | 얼굴이 거의 안 잡힘 (기존 55→35 완화) |
| `low_motion` | `max_jaw_open < 0.08` 또는 `mouth_open_ratio < 2` | 입 움직임 거의 없음 (각도로 낮아진 턱벌림 감안) |
| `high` | `detect_rate >= 65` **그리고** `observed_sec >= 20` | 고신뢰 (기존 80→65 완화) |
| `medium` | 위 어디에도 안 걸림 | 기본값 |

`low_face`/`low_motion`이면 화면에서 **"이 영상은 씹기 분석에 적합하지 않아요"** 재촬영 안내.

---

## 6. 화면 표시용 해석 기준

- **분당 씹기 횟수 코멘트**: `< 25` → "조금 적은 편" / `> 45` → "활발한 편" / 그 외 → "보통 정도"
- 좌우 균형은 `left_pct`/`right_pct` 바로 표시.
- 모든 지표는 **겉으로 보이는 턱·입 움직임 기반 추정치**임을 명시(입안·혀 속 미관측, 웃음·말하기를 씹기로 오인 가능). **의료 진단·치료 아님.**

---

## 7. 한계·주의

- blendshape는 **정면 얼굴에 최적화** → 측면·기울어진 얼굴은 값이 낮게 나옴(v2 완화로 일부 보정).
- 웃음/말하기 시 `jawOpen`이 오르면 씹기로 오집계될 수 있음.
- `tongueOut`(설출)은 이유식 초기엔 자연스러운 반응 → **실험 지표**로만 취급, 레벨 판정에 미사용.
- 손 지표(HMC·SFR 등 특허 15지표 중 손 관련)는 이 데모 얼굴 추출기 범위 밖.

---

### 관련 파일
- `데모버전/index.html` — 배포용 데모(이 문서의 대상)
- `chew_extract_local.py` — 동일 히스테리시스(0.15/0.06) 로컬 추출기
- `chew15_extract.py` — 특허 BTDS 15지표 시제품 추출기(신뢰 등급 A~E)
