# 배포 규약과 기록

## 규약 — 프로덕션은 `main` 푸시로만

**`vercel --prod` 를 쓰지 않는다.**

Vercel 에는 서로를 모르는 배포 경로가 둘 있고, 둘 다 `chewstep.com` 별칭 하나를
가리키게 만든다. **나중에 실행된 쪽이 이긴다.**

| 경로 | 무엇을 올리나 | 브랜치 설정을 보나 |
|---|---|---|
| GitHub 연동 (`source=git`) | `main` 브랜치의 커밋 | 본다 (Production Branch = `main`) |
| `vercel --prod` (`source=cli`) | **지금 이 폴더에 있는 파일** | **안 본다** |

`--prod` 는 "이 폴더를 프로덕션으로" 라는 명시적 지시라서, 폴더가 프로덕션 브랜치가
아니어도 Vercel 은 묻지 않고 그대로 올린다. 경고가 없다.

### 배포 절차
1. `main` 에 커밋하고 푸시한다. 끝이다. git 연동이 프로덕션에 올린다.
2. 기능 브랜치 작업은 푸시하면 프리뷰가 만들어진다. 프리뷰는 **Deployment Protection(SSO)**
   뒤에 있어 로그인 없이 열면 사이트가 아니라 Vercel 로그인 페이지가 나온다 —
   "반영이 안 됐다"로 착각하기 쉬운 지점이다.
3. 라이브 검증은 배포 후 실제 도메인에 하네스를 돌린다:
   `APP_URL=https://chewstep.com node verify/guide-board-check.mjs`
4. 되돌릴 때는 Vercel 대시보드에서 직전 프로덕션 배포를 Promote 한다. 즉시 적용된다.

---

## 2026-08-11 — 브랜치 갈라짐 정리 + 오해와 진실 복구

`main` = 라이브 = Vercel 프로덕션 브랜치 설정으로 일치시켰다. (`037fc3b`)

### 무슨 일이었나
`main` 과 `feat/evidence-based-report` 가 2026-07-28(`6bb2faa`)에 갈라진 뒤 양쪽에서
같은 작업을 중복으로 해왔다(가이드 3페이지·익명 인증·여정 로깅·밥태기 카피가 서로
다른 해시로 존재). 그 상태에서 배포가 엇갈렸다.

| 시각(KST) | 방식 | 브랜치 | 결과 |
|---|---|---|---|
| 08-10 08:41 | git | main | `myths.html` 신설, 배포됨 |
| 08-10 10:03 | git | main | 띠배너 시안 B |
| 08-10 10:52 | git | main | 히어로 3버튼 — **오해와 진실이 라이브였다** |
| 08-10 15:00 | **cli** | feat/evidence-based-report | **덮음** → `myths.html` 404 |

오후에 보호자 온보딩을 하려고 브랜치를 바꿨고, 그 브랜치에는 `myths.html` 이 없으니
작업 폴더에서도 사라진 상태였다(git 의 정상 동작). 그 상태에서 `vercel --prod` 를
실행해, 파일이 없는 트리가 프로덕션이 됐다.

프로덕션 브랜치 설정은 줄곧 `main` 이었다. 즉 라이브(브랜치 내용)와 설정(`main`)이
어긋난 채였고, 그래서 `main` 에 무엇을 푸시해도 화면이 안 바뀌었다.

### 어떻게 고쳤나
`main` 을 그대로 올리면 라이브에서 돌던 것이 되돌아간다 — `demo.html` 302KB→221KB
(관찰사실 추출기·근거 기반 처방·C단계 폼·촬영 직후 태그·임시저장·품질 게이팅),
`report.html` 12.6KB→8.2KB, coach 엣지함수 v4, 씹기 영상·음성 융합, 보호자 초대코드.
그래서 반대로 라이브 트리를 `main` 에 얹고 오해와 진실만 그 위에 이식했다.

- `54d55f8` `myths.html` 이식 + 홈 진입 + sitemap·llms. 명칭 "아이 식습관, 오해와 진실"
- `9de3f2f` 가이드 게시판에 여정 로그(글 9편 + 목록). 로그가 0건이던 구간이다.
  체류시간 UI(읽는 시간 칩·이어서 읽기)는 넣지 않았다 — 로그만 남으면 된다는 결정.
- `1b690e4` 병합. 트리는 라이브와 동일(`-s ours` + `read-tree --reset`),
  `main` 기존 17개 커밋은 히스토리에 남는다.
- `037fc3b` 진입부를 띠배너 3개로. 히어로는 주 CTA 하나만 — 배너와 버튼에 같은
  링크를 겹쳐 두면 계측 라벨이 중복돼 어디서 눌렸는지 못 가린다.

### 검증
- `guide-board-check` 로컬 267/267 PASS · **라이브 chewstep.com 267/267 PASS**
- `journey_events` 실기록 확인 — 하네스 25건이 `meta.is_test=true` 로 갈라지고,
  그 뒤 들어온 실사용자 7건은 표식 없이 정상 기록
- 회귀 없음 확인 — 라이브 `demo.html` 301,845 bytes, `report.html` 12,602 bytes,
  `/homecheck.html`·`/redeem.html`·`/report.html` 모두 200

### 되돌릴 지점
- `main` 직전 상태: tag `main-20260810-af4de1c`
- 직전 프로덕션 배포: `chewstep-demo-avhj56ond` (08-10 15:00, `4c14990`)
- 08-10 미커밋 작업분: branch `wip/coaching-no-mixing`
  (숨겨 섞기·형태 낮추기 권고 제거 — **임상 검토 전까지 배포하지 않는다**)

### 이어서 고친 것 (`58d3445`)
- **홈 진입부** — 띠 하나에 3개를 묶으니 주 CTA(데모)가 묻혔다. 데모는 히어로 버튼,
  상담·오해와진실은 그 아래 **독립 카드 2개**(테두리·라운드·그림자 각각, 12px 간격).
  배너에서 데모를 빼 링크 중복도 없앴다. 라벨(`entry3:*`)은 그대로 — 배치가 네 번
  바뀌었어도 측정 축은 이어진다. `entry3:demo` 는 이제 히어로 버튼에 붙는다.
- **가이드 내비** — `/guide/` 에서 오해와 진실로 돌아갈 길이 없었다(랜딩→글 단방향).
  목록 + 글 9편의 상단 내비·모바일 메뉴 양쪽에 링크 추가. `myths.html` 에도 '가이드' 추가.
- **`llms.txt` 글자 손상 25곳 복구.** 라이브에도 깨진 채 나가 있었다. 전부 원문 대조:
  `'교홍'`→`'균형' 51.00점`(korea-vegetables 의 균형 51.00·절제 62.45·실천 66.46),
  `오즐미`→`오즈비`(eating-speed 5회 표기), `한란드`→`핀란드`, `이유기보출식`→`이유기보충식`,
  `질볅관리잭`→`질병관리청`, `츠소`→`채소` 등. 9편 제목은 h1 과 전건 대조.
  korea-guideline 제목도 h1 과 맞췄다(`맞춘`→`맞춰`). **추측으로 고친 것은 없다.**

검증: 로컬 278/278 · **라이브 chewstep.com 278/278 PASS**

### 남은 것
- `login.html`·`report.html` 에 `journey.js` 없음 — 이 두 페이지는 로그가 안 남는다.
- `feat/evidence-based-report` 는 이제 `main` 보다 뒤처졌다. 이어서 작업하려면
  `main` 을 병합해 오거나 `main` 에서 새로 분기한다.
- `wip/coaching-no-mixing` 임상 검토 대기.

---

## 2026-07-29 릴리스

(이전 기록 — `origin/main` 의 `58673a8` 참조)
